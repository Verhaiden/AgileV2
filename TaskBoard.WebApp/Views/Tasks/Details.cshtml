@model TaskDetailsViewModel

@{
    ViewBag.Title = "Wyświetl szczegóły";
}

<link href="~/css/style.css" rel="stylesheet" type="text/css" />

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<table class="task-details">
    <tr class="task-id">
        <th>ID</th>
        <td>@Model.Id</td>
    </tr>
    <tr class="title">
        <th>Tytuł</th>
        <td>@Model.Title</td>
    </tr>
    <tr class="description">
        <th>Opis</th>
        <td>@Model.Description</td>
    </tr>
    <tr class="board">
        <th>Tablica</th>
        <td>@Model.Board</td>
    </tr>
    <tr class="board">
        <th>Właściciel</th>
        <td>@Model.Owner</td>
    </tr>
    <tr class="dateCreated">
        <th>Data utworzenia</th>
        <td>@Model.CreatedOn</td>
    </tr>
    @if (Model.Owner == this.User.Identity.Name)
    {
        <tr class="actions">
            <th>Actions</th>

            <td>
                <a asp-controller="Tasks" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-secondary">Edytuj</a>
                <a asp-controller="Tasks" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-secondary">Usuń</a>
            </td>
        </tr>
    }
</table>