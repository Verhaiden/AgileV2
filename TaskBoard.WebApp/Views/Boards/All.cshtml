@model IEnumerable<BoardViewModel>

@{
    ViewData["Title"] = "Stan projektu";
}

<link href="~/css/style.css" rel="stylesheet" type="text/css" />

<h2 class="text-center" style="color: black; padding: 10px">@ViewBag.Title</h2>
<hr/>

<div class="tasks-grid">
    @if (!Model.Any())
    {
        <h3 class="text-center">Brak zadań!</h3>
    }
    else
    {
        @foreach (var board in Model)
        {
            <div class="task">
                <h1>@board.Name</h1>
                @if (board.Tasks.Any())
                {
                    @await Html.PartialAsync("~/Views/Shared/_TaskPartial.cshtml", board.Tasks)
                }
            </div>
        }
    }
</div>
